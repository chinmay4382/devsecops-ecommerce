# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Safe Execution Mode

Before taking any action, briefly explain what you're about to do in 1-2 simple sentences — use plain language, say WHY not just WHAT, then proceed. Always prefer clear reasoning before action.

## Project

`devsecops-ecommerce` is a new, currently empty repository. No application code, build system, or infrastructure has been committed yet.

When code is added, update this file with:
- **Build/run/test commands** (e.g. `npm run dev`, `make test`, `docker compose up`)
- **Architecture overview** — services, their responsibilities, and how they interact
- **Security tooling** — SAST/DAST tools, secrets scanning, dependency audit commands, and where they run (CI vs. pre-commit)
